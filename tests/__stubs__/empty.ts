// Empty stub: `server-only`/`client-only` are build-guards with no runtime
// behavior; they just fail to resolve under Vitest. Aliased here so integration
// tests can import server modules (route handlers) directly.
export {}
