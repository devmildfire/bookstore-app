import * as React from 'react';
import { ClassNameProps } from '@/types/className';

type SortProps = ClassNameProps

const Sort: React.FC<SortProps> = () => {
  return null;
};

export default Sort;
