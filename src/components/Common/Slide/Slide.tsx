import React from 'react';
// import { SwiperSlide, SwiperSlideProps } from 'swiper/react';
import StyledSlide from './styles';

// const Slide = (props: SwiperSlideProps): React.ReactElement => {
//   const { children, ...params } = props;
//   return <StyledSlide {...params}>{children}</StyledSlide>;
// };

// /* Свайпер определяет слайды по их имени */
// Slide.displayName = SwiperSlide.displayName;

// export default Slide;
