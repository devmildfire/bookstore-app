{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    "docker:pinDigests",
    "helpers:pinGitHubActionDigests"
  ],
  "prConcurrentLimit": 8,
  "prHourlyLimit": 0,
  "rangeStrategy": "pin",
  "lockFileMaintenance": { "enabled": true, "schedule": ["at any time"], "automerge": true },
  "vulnerabilityAlerts": { "labels": ["security"], "automerge": true },
  "major": { "dependencyDashboardApproval": true },
  "packageRules": [
    { "matchDepTypes": ["engines"], "enabled": false },
    {
      "description": "Autonomous loop: auto-merge npm (dev+runtime) on green — the real suite (unit+integration+e2e on a live stack) gates them. MAJORS don't auto-open: they wait for dashboard-approval (`major` config above), because breaking majors would just churn CI + spam failure alerts. Once you approve one, it auto-merges on green like the rest.",
      "matchManagers": ["npm"],
      "automerge": true
    },
    {
      "description": "Group ALL non-major npm updates into ONE PR. Every npm PR touches package-lock.json, so multiple open at once conflict the moment one merges (the queue clogs). One grouped PR = one lockfile change in flight = no sibling conflicts. Majors stay individual for blast-radius isolation.",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["patch", "minor", "pin", "digest"],
      "groupName": "npm (non-major)"
    },
    { "matchManagers": ["github-actions"], "automerge": true },
    { "matchUpdateTypes": ["lockFileMaintenance"], "automerge": true },
    {
      "description": "Default for docker: manual (untested — monitoring, cloudflared, postgrest, meta). CI can't verify them yet; a weekly digest pings so they don't pile silently. Overridden below for candidate-tested images.",
      "matchDatasources": ["docker"],
      "automerge": false,
      "addLabels": ["needs-review"]
    },
    {
      "description": "Docker majors: manual regardless — larger config surface than the Tier-1 candidate test covers.",
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "addLabels": ["needs-review"]
    },
    {
      "description": "Infra images WITH a Tier-1 candidate test: auto-merge patch/minor/digest, gated by the REQUIRED 'Tier-1 candidate boot' check (boots the candidate with real config and probes health). Ordered AFTER the general docker rules so it wins (Renovate: later rule overrides). Majors excluded → stay manual via the rule above.",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "nginx",
        "kong/kong",
        "grafana/grafana",
        "prom/prometheus",
        "prom/node-exporter",
        "prom/pushgateway",
        "prom/alertmanager"
      ],
      "matchUpdateTypes": ["patch", "minor", "digest"],
      "automerge": true
    },
    {
      "description": "FROZEN stateful trio: auto-update is unsafe (migration/data coupling; storage has NO restore source, so 'roll back on failure' is impossible). Bumped only via a rehearsed restore.",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "public.ecr.aws/supabase/postgres",
        "public.ecr.aws/supabase/gotrue",
        "public.ecr.aws/supabase/storage-api"
      ],
      "matchUpdateTypes": ["major", "minor", "patch", "digest"],
      "enabled": false
    }
  ],
  "platformAutomerge": true
}
