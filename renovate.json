{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    "docker:pinDigests",
    "helpers:pinGitHubActionDigests"
  ],
  "prConcurrentLimit": 8,
  "prHourlyLimit": 0,
  "rangeStrategy": "pin",
  "rebaseWhen": "conflicted",
  "lockFileMaintenance": { "enabled": true, "schedule": ["at any time"], "automerge": true },
  "vulnerabilityAlerts": { "labels": ["security"], "automerge": true },
  "packageRules": [
    { "matchDepTypes": ["engines"], "enabled": false },
    {
      "description": "Label every major so the failure alert can flag it as a BREAKING change needing a decision. Majors still auto-attempt (they fail CI + alert if they break) — deliberately NOT gated.",
      "matchUpdateTypes": ["major"],
      "addLabels": ["major"]
    },
    {
      "description": "Autonomous loop: auto-merge all npm (dev+runtime, incl. major) on green. The real suite (unit+integration+e2e on a live stack) is the gate — a breaking bump fails CI and never merges.",
      "matchManagers": ["npm"],
      "automerge": true
    },
    {
      "description": "Group ALL non-major npm updates into ONE PR. Every npm PR touches package-lock.json, so multiple open at once conflict the moment one merges (the queue clogs). One grouped PR = one lockfile change in flight = no sibling conflicts. Majors stay individual for blast-radius isolation.",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["patch", "minor", "pin", "digest"],
      "groupName": "npm (non-major)"
    },
    { "matchManagers": ["github-actions"], "automerge": true },
    { "matchUpdateTypes": ["lockFileMaintenance"], "automerge": true },
    {
      "description": "Default for docker: manual (untested — monitoring, cloudflared, postgrest, meta). CI can't verify them yet; a weekly digest pings so they don't pile silently. Overridden below for candidate-tested images.",
      "matchDatasources": ["docker"],
      "automerge": false,
      "addLabels": ["needs-review"]
    },
    {
      "description": "Docker majors: manual regardless — larger config surface than the Tier-1 candidate test covers.",
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "addLabels": ["needs-review"]
    },
    {
      "description": "Infra images WITH a candidate test (Tier-1 config-boot or Tier-2 DB-coupled): auto-merge patch/minor/digest, gated by the REQUIRED 'Tier-1 candidate boot' check (boots the candidate with real config/DB and probes health). Ordered AFTER the general docker rules so it wins (Renovate: later rule overrides). Majors excluded → stay manual via the rule above.",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "nginx",
        "kong/kong",
        "grafana/grafana",
        "prom/prometheus",
        "prom/node-exporter",
        "prom/pushgateway",
        "prom/alertmanager",
        "public.ecr.aws/supabase/postgrest",
        "public.ecr.aws/supabase/postgres-meta",
        "prometheuscommunity/postgres-exporter"
      ],
      "matchUpdateTypes": ["patch", "minor", "digest"],
      "automerge": true
    },
    {
      "description": "Formerly-frozen stateful trio (postgres/gotrue/storage-api): now gated by the Tier-3 MIGRATION-COMPAT candidate test (auth/storage: base migrates a prod-image DB → candidate applies its boot migration + serves → base still serves the migrated schema = expand-compat; db: candidate postgres must start on the base image's data dir with data intact). Auto-merge patch/minor/digest ON GREEN. NOTE: auto-merge lands the tested-good version in the REPO only — the prod APPLY stays the manual rehearsed runbook (backup → per-service swap → verify → rollback), because migrations/data must be applied by a human with a fresh backup. Ordered AFTER the general docker rules so it wins.",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "public.ecr.aws/supabase/postgres",
        "public.ecr.aws/supabase/gotrue",
        "public.ecr.aws/supabase/storage-api"
      ],
      "matchUpdateTypes": ["patch", "minor", "digest"],
      "automerge": true
    },
    {
      "description": "Trio MAJORS stay manual: a postgres major needs pg_upgrade / logical-replication (not a compose swap), and the Tier-3 gate will correctly FAIL it (candidate can't start on the old data dir). Labeled for an explicit human decision + the MAJOR Telegram alert.",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "public.ecr.aws/supabase/postgres",
        "public.ecr.aws/supabase/gotrue",
        "public.ecr.aws/supabase/storage-api"
      ],
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "addLabels": ["needs-review", "major"]
    }
  ],
  "platformAutomerge": true
}
